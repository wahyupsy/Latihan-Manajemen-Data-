

# Mendefinisikan 

a <- "Aku"
b = 9
c <- 2

d <- b + c
b + a
e <- "Kita"

paste0(a,e)

f <- paste0(a, "-", e)

#install package
install.packages("psych")
- Untuk install packages yang tersedia di R
library("psych")
- Untuk melihat apakah pakcages yang di install sudah aktif atau belum.

===================================================================

# Manajemen Data
#df <- read.csv("Twisk.csv")
#Df = Nama/sebutan file
Read.csv untuk melakukan data input beri tanda kutip 
untuk memanggil spesifik nama file
- Pindahkan file ke directory file R disimpan.

#View(df)#
Untuk melihat file yang sudah di input

